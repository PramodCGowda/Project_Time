import { TaskFavoriteDirective } from './task-favorite.directive';

describe('TaskCompletedDirective', () => {
  it('should create an instance', () => {
    const directive = new TaskFavoriteDirective();
    expect(directive).toBeTruthy();
  });
});
